document.querySelectorAll('table.table').forEach(t=>{t.querySelectorAll('thead th').forEach((h,i)=>{h.style.cursor='pointer';h.title='مرتب‌سازی';h.addEventListener('click',()=>{let rows=[...t.querySelectorAll('tbody tr')],asc=h.dataset.asc!=='1';rows.sort((a,b)=>a.cells[i].innerText.localeCompare(b.cells[i].innerText,'fa',{numeric:true}));if(!asc)rows.reverse();rows.forEach(r=>t.tBodies[0].appendChild(r));h.dataset.asc=asc?'1':'0';});});});document.querySelectorAll('[data-table-filter]').forEach(input=>input.addEventListener('input',()=>{let t=document.querySelector(input.dataset.tableFilter),q=input.value.toLocaleLowerCase();if(t)t.querySelectorAll('tbody tr').forEach(r=>r.style.display=r.innerText.toLocaleLowerCase().includes(q)?'':'none');}));